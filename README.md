# codex-test1

Chrome extension for auto-filling procurement system forms using templates.

## Development

Extension source is located in `extension/` directory. Load it as an unpacked extension in Chrome to use.

Templates can be managed via the extension options page and applied through the popup on supported pages.
